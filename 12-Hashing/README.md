# Hashing

### Two Sum (`bai1.cpp`)

- **Concept:** Dùng `unordered_map` để tìm hai phần tử có tổng bằng target.
- **Approach:** Với mỗi giá trị, kiểm tra phần bù `target - a[i]` đã xuất hiện chưa.
- **Time:** O(n) trung bình.
- **Space:** O(n)
