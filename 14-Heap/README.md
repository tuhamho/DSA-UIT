# Heap / Priority Queue

### Bài tập

1. Cài đặt min heap bằng mảng.
2. Cài đặt max heap bằng mảng.
3. Dùng `priority_queue` tìm `k` phần tử lớn nhất.
4. Heap sort.
5. Trộn nhiều mảng đã sắp xếp.

Mục tiêu: hiểu heapify và thao tác ưu tiên `O(log n)`.
